apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    database: sumodb
    user: sumouser
    secureJsonData:
      password: sumopass
    jsonData:
      sslmode: "disable"
      postgresVersion: 1500
      timescaledb: false
    editable: true
    isDefault: true
